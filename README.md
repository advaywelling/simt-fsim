# simt-fsim
A functional SIMT simulator that executes GPU kernel IR (toy ISA, PTX) lane-by-lane with correct divergence handling, reporting SIMD utilization and memory coalescing - not cycle-accurate
